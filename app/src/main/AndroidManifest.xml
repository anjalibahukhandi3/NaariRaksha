<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <application android:name=".NaariRakshaApplication"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NaariRaksha"
        tools:targetApi="31">
        <meta-data android:name="com.google.android.gms.auth.api.signin.clientId" android:value="1042239661296-6s4rg6gta2lko08f1mkfpetd3bscq76u.apps.googleusercontent.com" />
        <meta-data android:name="com.google.android.geo.API_KEY" android:value="AIzaSyBsi-4tCYVcCOY8_AOHmOGrKcjaAced9S0" />

        <!-- HOME -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <!-- SPLASH (ONLY LAUNCHER) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MAIN -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- LOGIN -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- CONTACTS -->
        <activity
            android:name=".ContactsActivity"
            android:exported="false" />

        <!-- HELPLINES -->
        <activity
            android:name=".HelplinesActivity"
            android:exported="false" />

        <!-- FAKE CALL (FIXED) -->
        <activity
            android:name=".ui.FakeCallActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

    </application>

</manifest>

